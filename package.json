{
  "private": true,
  "scripts": {
    "dev": "npm --prefix RAIKU run dev",
  "build": "npm --prefix RAIKU run build && node ./scripts/copy-dist.js",
    "start": "npm --prefix RAIKU run preview"
  }
}
